{% extends 'base.html.twig' %}

{% block title %}InventaireArme{% endblock %}

{% block body %}
    <h1>InventaireArme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventaire_arme.id }}</td>
            </tr>
            <tr>
                <th>NomItemInventaire</th>
                <td>{{ inventaire_arme.nomItemInventaire }}</td>
            </tr>
            <tr>
                <th>DescriptionItemInventaire</th>
                <td>{{ inventaire_arme.descriptionItemInventaire }}</td>
            </tr>
            <tr>
                <th>PoidsItemInventaire</th>
                <td>{{ inventaire_arme.poidsItemInventaire }}</td>
            </tr>
            <tr>
                <th>BeneficeMaluceInventaire</th>
                <td>{{ inventaire_arme.beneficeMaluceInventaire }}</td>
            </tr>
            <tr>
                <th>ValeurInventaire</th>
                <td>{{ inventaire_arme.valeurInventaire }}</td>
            </tr>
            <tr>
                <th>DegatArmeInventaire</th>
                <td>{{ inventaire_arme.degatArmeInventaire }}</td>
            </tr>
        <tr>
            <th>Associer A :</th>

            {% for personnage in inventaire_arme.personnages %}
            <td>
            {{ personnage.nomPersonnage }}
            </td>

            {% endfor %}
        </tr>
        </tbody>
    </table>

    <a href="{{ path('inventaire_arme_index') }}">back to list</a>

    {#<a href="{{ path('inventaire_arme_edit', {'id': inventaire_arme.id}) }}">edit</a>#}

    {#{{ include('inventaire_arme/_delete_form.html.twig') }}#}
{% endblock %}
