{% extends 'base.html.twig' %}

{% block title %}TypeTalent{% endblock %}

{% block body %}
    <h1>TypeTalent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_talent.id }}</td>
            </tr>
            <tr>
                <th>NomTypeTalent</th>
                <td>{{ type_talent.nomTypeTalent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_talent_index') }}">back to list</a>

    <a href="{{ path('type_talent_edit', {'id': type_talent.id}) }}">edit</a>

    {{ include('type_talent/_delete_form.html.twig') }}
{% endblock %}
