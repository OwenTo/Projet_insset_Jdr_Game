<form method="post" action="{{ path('region_langue_delete', {'id': region_langue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region_langue.id) }}">
    <button class="btn">Delete</button>
</form>
