{% extends 'base.html.twig' %}

{% block title %}Détail du Matériel{% endblock %}

{% block body %}
    <h1 class="text-center">Détail du Matériel</h1>
    <div class="  mb-5 mt-5  " style="margin-right: 15%;margin-left: 15%">
        <div class=" centerText  col-mb-3 mr-2 card  shadow p-3 mb-5  rounded"
             style="margin-top: 10px; background-color: beige;">
            <table class="table">
                <tbody>
                {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ materiel.id }}</td>#}
                {#</tr>#}
                <tr>
                    <th>nom du matériel</th>
                    <td>{{ materiel.typeMateriel }}</td>
                </tr>
                </tbody>
            </table>
        </div>
        <div class="row ml-2">


            <a class="btn btn-primary mr-1" href="{{ path('materiel_index') }}">
                <i class="fas fa-undo"></i>
            </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="btn btn-primary mr-1" href="{{ path('materiel_edit', {'id': materiel.id}) }}">
                    <i class="fas fa-edit"></i>
                </a>

                {{ include('materiel/_delete_form.html.twig') }}

            {% endif %}
        </div>
    </div>
{% endblock %}
