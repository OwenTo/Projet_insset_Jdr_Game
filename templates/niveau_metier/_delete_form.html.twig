<form method="post" action="{{ path('niveau_metier_delete', {'id': niveau_metier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau_metier.id) }}">
    <button class="btn">Delete</button>
</form>
