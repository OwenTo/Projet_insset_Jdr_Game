{% extends 'base.html.twig' %}

{% block title %}Partie index{% endblock %}

{% block body %}
    <h1>Partie index</h1>

    <table class="table">
        <thead>
            <tr>
                {#<th>Id</th>#}
                <th>NomPartie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for partie in parties %}
            <tr>
                {#<td>{{ partie.id }}</td>#}
                <td>{{ partie.nomPartie }}</td>
                <td>
                    <a href="{{ path('partie_show', {'id': partie.id}) }}">shoffw</a>
                    {#<a href="{{ path('partie_edit', {'id': partie.id}) }}">edit</a>#}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('partie_new',{'idUser':app.user.id}) }}">Create new</a>
    <a href="{{ path('partie_new_user',{'idUser':app.user.id}) }}">Create new(user)</a>
{% endblock %}
