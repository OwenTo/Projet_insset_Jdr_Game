{% extends 'base.html.twig' %}

{% block title %}CapaciteRacial{% endblock %}

{% block body %}
    <h1>CapaciteRacial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ capacite_racial.id }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ capacite_racial.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('capacite_racial_index') }}">back to list</a>

    <a href="{{ path('capacite_racial_edit', {'id': capacite_racial.id}) }}">edit</a>

    {{ include('capacite_racial/_delete_form.html.twig') }}
{% endblock %}
