{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Inscription</h1>

    <div class=" centerText container    card  shadow p-3 mb-5  rounded" style="margin-top: 10px; background-color: beige;">
        {{ form_start(form) }}
        <div class="form-group row pl-2 pr-2">{{ form_label(form.username,"Nom d'ultilisateur") }}
        {{ form_widget(form.username,{'attr':{'placeholder' :"Nom d'utilisateur",'class': 'form-control'}}) }}
        </div>
        <div class="form-group row pl-2 pr-2">{{ form_label(form.email,"E-mail") }}
        {{ form_widget(form.email,{'attr':{'placeholder' :"mail@mail.fr",'class': 'form-control'}}) }}
        </div>
        <div class="form-group row pl-2 pr-2">{{ form_label(form.password,"'Mot de passe") }}
        {{ form_widget(form.password,{'attr':{'placeholder' :"Mot de passe ",'class': 'form-control'}}) }}
        </div>
        <div class="form-group row pl-2 pr-2">{{ form_label(form.confirm_password,"Corfirmation de mot de passe") }}
        {{ form_widget(form.confirm_password,{'attr':{'placeholder' :"Confirmation mot de passe",'class': 'form-control'}}) }}
        </div>

        <button type="submit" class="btn btn-primary mb-2">
            {% if editMode %}
                Enregistrer
            {% else %}
                Validation

            {% endif %}
        </button>

    </div>

    {##}
    {#{{ form_start(form) }}#}
    {#{{ form_row(form.username, {'label':'Nom d\'ultilisateur','attr':{'placeholder' :"Nom d'utilisateur"}}) }}#}
    {#{{ form_row(form.email, {'label':'E-mail','attr':{'placeholder':'Adresse mail'}}) }}#}
    {#{% if editMode %}#}
        {#<p>veuillez réinserer votre mot de passe , pour plus de securiter</p>#}
    {#{% endif %}#}
    {#{{ form_row(form.password, {'label':'Mot de passe','attr':{'placeholder' :"Mot de passe"}} ) }}#}
    {#{{ form_row(form.confirm_password, {'label':'Corfirmation de mot de passe','attr':{'placeholder' :''}} ) }}#}
    {#{{ form_row(form.stockage) }}#}
    {#{{ form_widget(form.stockage) }}#}

    {{ form_end(form) }}
{% endblock body %}