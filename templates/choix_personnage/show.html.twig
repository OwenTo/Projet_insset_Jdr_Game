{% extends 'base.html.twig' %}

{% block title %}ChoixPersonnage{% endblock %}

{% block body %}
    <h1>ChoixPersonnage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix_personnage.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('choix_personnage_index') }}">back to list</a>

    <a href="{{ path('choix_personnage_edit', {'id': choix_personnage.id}) }}">edit</a>

    {{ include('choix_personnage/_delete_form.html.twig') }}
{% endblock %}
