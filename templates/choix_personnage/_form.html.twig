{#{{ form_start(form) }}#}



    {#{{ form_widget(form) }}#}
    {#<button class="btn">{{ button_label|default('Save') }}</button>#}
{#{{ form_end(form) }}#}

{% for personnage in personnages %}
    <form name="personnage" method="post">
        <input type="hidden" name="idPersonnage" value="{{ personnage.id }}">
        <label>{{ personnage.nomPersonnage }}</label>
        <button name="sendPersonnageChoix" type="submit" value="1"></button>
    </form>
{% endfor %}