{% extends 'base.html.twig' %}

{% block title %}TypeRace{% endblock %}

{% block body %}
    <h1>TypeRace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_race.id }}</td>
            </tr>
            <tr>
                <th>TypeRace</th>
                <td>{{ type_race.typeRace }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_race_index') }}">back to list</a>

    <a href="{{ path('type_race_edit', {'id': type_race.id}) }}">edit</a>

    {{ include('type_race/_delete_form.html.twig') }}
{% endblock %}
