<form method="post" action="{{ path('inventaire_magie_delete', {'id': inventaire_magie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inventaire_magie.id) }}">
    <button class="btn">Delete</button>
</form>
