<form method="post" action="{{ path('magie_delete', {'id': magie.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimé cette magie ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ magie.id) }}">
    <button class="btn btn-primary"> <i class="fas fa-trash-alt"></i></button>
</form>
