{% extends 'base.html.twig' %}

{% block title %}TypeDes{% endblock %}

{% block body %}
    <h1>TypeDes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_de.id }}</td>
            </tr>
            <tr>
                <th>Des</th>
                <td>{{ type_de.des }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_des_index') }}">back to list</a>

    <a href="{{ path('type_des_edit', {'id': type_de.id}) }}">edit</a>

    {{ include('type_des/_delete_form.html.twig') }}
{% endblock %}
