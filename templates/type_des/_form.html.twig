{{ form_start(form) }}
<div class="form-group mb-1 container-fluid">
    {{ form_label(form.des,'Dés') }}
    {{ form_widget(form.des,{'attr':{'class':'form-control'}}) }}
    {{ form_errors(form.des) }}
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
