<form method="post" action="{{ path('monnaie_delete', {'id': monnaie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monnaie.id) }}">

   <div class="row m-2">
       <div class="pr-2">
           <a class="btn btn-primary mb-1" href="{{ path('monnaie_index') }}">
               <i class="fas fa-backward"></i>
           </a>
       </div>
       <div class="">
           <button class="btn btn-primary">
               <i class="fas fa-trash-alt"></i>
           </button>
       </div>
   </div>

</form>
