{% extends 'base.html.twig' %}

{% block title %}CompagnonType{% endblock %}

{% block body %}
    <h1>CompagnonType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compagnon_type.id }}</td>
            </tr>
            <tr>
                <th>TypeCompagnon</th>
                <td>{{ compagnon_type.typeCompagnon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compagnon_type_index') }}">back to list</a>

    <a href="{{ path('compagnon_type_edit', {'id': compagnon_type.id}) }}">edit</a>

    {{ include('compagnon_type/_delete_form.html.twig') }}
{% endblock %}
