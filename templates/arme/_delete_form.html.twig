<form method="post" action="{{ path('arme_delete', {'id': arme.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimé cette arme ??');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arme.id) }}">
    <button class="btn btn-primary"> <i class="fas fa-trash-alt"></i></button>
</form>
