<form method="post" action="{{ path('caracteristique_principal_delete', {'id': caracteristique_principal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caracteristique_principal.id) }}">
    <button class="btn">Delete</button>
</form>
