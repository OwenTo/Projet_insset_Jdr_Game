{% extends 'base.html.twig' %}

{% block title %}CaracteristiquePrincipal index{% endblock %}

{% block body %}
    <h1>CaracteristiquePrincipal index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCaracteristique</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for caracteristique_principal in caracteristique_principals %}
            <tr>
                <td>{{ caracteristique_principal.id }}</td>
                <td>{{ caracteristique_principal.nomCaracteristique }}</td>
                <td>
                    <a href="{{ path('caracteristique_principal_show', {'id': caracteristique_principal.id}) }}">show</a>
                    <a href="{{ path('caracteristique_principal_edit', {'id': caracteristique_principal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('caracteristique_principal_new') }}">Create new</a>
{% endblock %}
