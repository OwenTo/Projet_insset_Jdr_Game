<form method="post" action="{{ path('caracteristique_secondaire_delete', {'id': caracteristique_secondaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caracteristique_secondaire.id) }}">
    <button class="btn">Delete</button>
</form>
