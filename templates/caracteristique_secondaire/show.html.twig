{% extends 'base.html.twig' %}

{% block title %}CaracteristiqueSecondaire{% endblock %}

{% block body %}
    <h1>CaracteristiqueSecondaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caracteristique_secondaire.id }}</td>
            </tr>
            <tr>
                <th>NomCaracteristique</th>
                <td>{{ caracteristique_secondaire.nomCaracteristique }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('caracteristique_secondaire_index') }}">back to list</a>

    <a href="{{ path('caracteristique_secondaire_edit', {'id': caracteristique_secondaire.id}) }}">edit</a>

    {{ include('caracteristique_secondaire/_delete_form.html.twig') }}
{% endblock %}
