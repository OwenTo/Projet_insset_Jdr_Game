{% extends 'base.html.twig' %}

{% block title %}CaracteristiqueSecondaire index{% endblock %}

{% block body %}
    <h1>CaracteristiqueSecondaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCaracteristique</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for caracteristique_secondaire in caracteristique_secondaires %}
            <tr>
                <td>{{ caracteristique_secondaire.id }}</td>
                <td>{{ caracteristique_secondaire.nomCaracteristique }}</td>
                <td>
                    <a href="{{ path('caracteristique_secondaire_show', {'id': caracteristique_secondaire.id}) }}">show</a>
                    <a href="{{ path('caracteristique_secondaire_edit', {'id': caracteristique_secondaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('caracteristique_secondaire_new') }}">Create new</a>
{% endblock %}
